# Trajectory
This repository contains routines for kinetic models, their data generation, and training neural networks to create an efficient model for trajectory finding in plausible time frame.

Run the following command to install the dependencies -

`!pip install z3-solver pandas matplotlib pulp gurobipy`
